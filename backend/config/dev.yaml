# Development Configuration
database:
  url: postgresql://postgres:postgres@db:5432/clerkk

auth0:
  domain: technotut.au.auth0.com
  api_audience: https://api.inbriefs.com

aws:
  region: ap-south-1

cors:
  allowed_origins:
    - http://localhost:5173

app:
  environment: development
  log_level: DEBUG
  debug: true
